import {
  LOGIN,
  SIGNUP,
  EDIT_BUDGET,
  ADD_TRANS,
  EDIT_TRANS,
  REMOVE_TRANS
} from "../actions/types";

export default (state = [], action) => {
  switch (action.type) {
    default:
      return state;
  }
};
